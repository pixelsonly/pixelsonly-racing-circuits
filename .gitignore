# Node
node_modules/
npm-debug.log*

# Build output (generated package / JSON emit)
build/
dist/

# OS / editor cruft
.DS_Store
._*
*.swp
.idea/
.vscode/

# Credentials — never commit (capture tooling lives in the private repo anyway)
.env
.env.*
!.env.example

# Track-map source files: fetched by `npm run fetch-map` from Wikimedia
# Commons, kept on disk for the Affinity tracing pass, never committed —
# only the hand-traced production map.svg ships in the repo.
tracks/*/.map-source.svg
